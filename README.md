Script made in node.js and playwright to automate passport applications.

How to run the app?
1. Clone the repository
2. Run npm install
3. Run the script with the following command: "node automateEsta.js"  
